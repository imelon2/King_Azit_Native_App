import QRCode from 'react-native-qrcode-svg';

// docs : https://www.npmjs.com/package/react-native-qrcode-svg
const QrCode = ({...props}) => {
    return (
        <QRCode {...props}/>
    )
}

export default QrCode